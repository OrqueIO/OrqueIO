<?xml version="1.0" encoding="UTF-8" ?>

<definitions id="definitions"
             targetNamespace="http://orqueio.io/schema/1.0/bpmn20"
             xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:orqueio="http://orqueio.io/schema/1.0/bpmn">
  
  <process id="FormPropertyHandlingProcess" isExecutable="true">
  
    <startEvent id="start">
      <extensionElements>
        <orqueio:formProperty id="speaker" variable="SpeakerName"/>
        <orqueio:formProperty id="duration" type="long"/>
        <orqueio:formProperty id="free" type="boolean" />
      </extensionElements>
    </startEvent>

    <sequenceFlow id="flow1" sourceRef="start" targetRef="task" />
    
    <userTask id="task"
              orqueio:assignee="kermit">
      <extensionElements>
        <orqueio:formProperty id="room" />
        <orqueio:formProperty id="duration" type="long"/>
        <orqueio:formProperty id="speaker" variable="SpeakerName" writable="false" />
        <orqueio:formProperty id="street" expression="#{address.street}" required="true" />
        <orqueio:formProperty id="free" type="boolean"/>
      </extensionElements>
    </userTask>

    <sequenceFlow id="flow2" sourceRef="task" targetRef="wait" />
    
    <receiveTask id="wait" />

  </process>
  
</definitions>
