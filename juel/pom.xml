<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>



  <parent>
    <groupId>io.orqueio.bpm</groupId>
    <artifactId>orqueio-database-settings</artifactId>
    <relativePath>../database</relativePath>
    <version>2.0.5</version>
  </parent>
  <description>orqueio-juel</description>

  <groupId>io.orqueio.bpm.juel</groupId>
  <artifactId>orqueio-juel</artifactId>
  <version>2.0.5</version>
  <packaging>jar</packaging>
  <name>Orque IO Platform - JUEL</name>

  <properties>
    <!-- We only wrap and maintain external code in here that has
    its own license header already -->
    <license.skip>true</license.skip>
    <!-- We shade artifacts into the jar, so we need to generate
    a dependency BOM for the license book -->
    <skip-third-party-bom>false</skip-third-party-bom>
    <!-- Using Jakarta Expression Language 4.0 for Java 8 compatibility -->
    <version.jakarta.el>4.0.0</version.jakarta.el>
  </properties>

  <dependencies>
    <dependency>
      <groupId>jakarta.el</groupId>
      <artifactId>jakarta.el-api</artifactId>
      <version>${version.jakarta.el}</version>
      <optional>true</optional>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <executions>
          <!-- Create the main JAR early so shade can consume it before the test phase -->
          <execution>
            <id>pre-shade-jar</id>
            <phase>process-test-classes</phase>
            <goals>
              <goal>jar</goal>
            </goals>
          </execution>
          <!-- Disable the default package-phase jar; maven-jar-plugin 3.4+ rejects
               replacing an artifact that shade already set at process-test-classes -->
          <execution>
            <id>default-jar</id>
            <phase>none</phase>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <executions>
          <!-- Create sources JAR at the same early phase so shade-early can process it -->
          <execution>
            <id>attach-sources</id>
            <phase>process-test-classes</phase>
            <goals>
              <goal>jar-no-fork</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <executions>
          <!-- Shade early so the shaded JAR is available to reactor dependents during mvn test -->
          <execution>
            <id>shade-early</id>
            <phase>process-test-classes</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <createSourcesJar>true</createSourcesJar>
              <shadeSourcesContent>true</shadeSourcesContent>
              <promoteTransitiveDependencies>true</promoteTransitiveDependencies>
              <artifactSet>
                <includes>
                  <include>jakarta.el:jakarta.el-api</include>
                </includes>
              </artifactSet>
              <relocations>
                <relocation>
                  <pattern>jakarta.el</pattern>
                  <shadedPattern>io.orqueio.bpm.impl.juel.jakarta.el</shadedPattern>
                </relocation>
              </relocations>
            </configuration>
          </execution>
          <!-- Disable the inherited package-phase shade to prevent double-shading -->
          <execution>
            <id>default</id>
            <phase>none</phase>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <licenses>
    <license>
      <name>The Apache Software License, Version 2.0</name>
      <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
    </license>
  </licenses>

  <organization>
    <name>Orque IO</name>
    <url>https://www.orqueio.io</url>
  </organization>

  <url>https://www.orqueio.io</url>

  <developers>
    <developer>
      <id>orqueio.io</id>
      <name>Orque IO Platform Community</name>
      <organization>orqueio.io</organization>
      <organizationUrl>https://www.orqueio.io</organizationUrl>
    </developer>
  </developers>

  <scm>
    <url>https://github.com/orqueio/orqueio</url>
    <connection>scm:git:git@github.com:orqueio/orqueio.git</connection>
    <developerConnection>scm:git:git@github.com:orqueio/orqueio.git</developerConnection>
    <tag>HEAD</tag>
  </scm>

</project>
