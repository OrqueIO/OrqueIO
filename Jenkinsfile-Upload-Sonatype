#!/usr/bin/env groovy

pipeline {

    agent any

    tools {
        jdk 'jdk17'
        maven 'maven'
    }



    options {
        buildDiscarder(logRotator(numToKeepStr: '5'))
    }



    stages {

    	stage('Upload') {
    		steps {
                sh "cd bom && mvn clean install -DskipTests"
                sh "cd connect/bom && mvn clean install -DskipTests"
                sh "cd commons/bom && mvn clean install -DskipTests"
                sh "cd commons/logging && mvn clean install -DskipTests"
                sh "cd test-utils/archunit && mvn clean install -DskipTests"
                sh "mvn clean deploy  -X -Pcentral-sonatype-publish -DskipTests -Dclirr.skip=true"
	    	}
	 	}
    }

    post {
        always {
            cleanWs()
        }
    }
}
