pipeline {
    agent any

    tools {
        maven 'maven'
    }

    stages {
        stage('SonarQube analysis') {
            when { branch "upload-sonatype" }
            environment {
                scannerHome = tool 'scannerHome'
            }
            steps {
                withSonarQubeEnv('AWS SONAR') {
                    sh """
                        mvn clean verify sonar:sonar \
                          -Pupload-sonatype-publish \
                          -Dsonar.projectKey=Orqueio
                    """
                }
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
