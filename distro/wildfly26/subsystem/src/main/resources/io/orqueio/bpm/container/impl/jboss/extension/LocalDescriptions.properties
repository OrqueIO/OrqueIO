orqueio-bpm-platform=Orqueio Platform subsystem.
orqueio-bpm-platform.add=Add the Orqueio Platform subsystem.
orqueio-bpm-platform.remove=Remove the Orqueio Platform subsystem.

orqueio-bpm-platform.process-engines=Process engines provided by the Orqueio Platform subsystem.

orqueio-bpm-platform.process-engine=A process engine.
orqueio-bpm-platform.process-engine.plugins.class=The entry class of a process engine plugin.
orqueio-bpm-platform.process-engine.plugins.properties=The properties to configure a process engine plugin.
orqueio-bpm-platform.process-engine.add=Add a process engine.
orqueio-bpm-platform.process-engine.remove=Remove a process engine.

orqueio-bpm-platform.process-engine.name=The unique name of the process engine.
orqueio-bpm-platform.process-engine.default=Marks the process engine as the default one. Only one process engine can be the default one.
orqueio-bpm-platform.process-engine.history-level=The history level used by the process engine. Defaults to 'audit'.
orqueio-bpm-platform.process-engine.configuration=The process engine configuration class which should be used. Defaults to 'io.orqueio.bpm.container.impl.jboss.config.ManagedJtaProcessEngineConfiguration'.
orqueio-bpm-platform.process-engine.datasource=The datasource which should be used for the process engine persistence.
orqueio-bpm-platform.process-engine.properties=Customize the process engine through various properties.
orqueio-bpm-platform.process-engine.plugins=Extend the process engine through various plugins.

orqueio-bpm-platform.job-executor=Global job executor.
orqueio-bpm-platform.job-executor.add=Add the job executor.
orqueio-bpm-platform.job-executor.remove=Remove the job executor.

orqueio-bpm-platform.job-executor.thread-pool-name=The name of the thread pool which is created as part of the job executor startup. Defaults to 'job-executor-tp'
orqueio-bpm-platform.job-executor.core-threads=The minimal number of threads alive. Defaults to '3'.
orqueio-bpm-platform.job-executor.max-threads=The max number of concurrently running threads. Defaults to '5'.
orqueio-bpm-platform.job-executor.queue-length=The length of the thread pool queue. Defaults to '10'.
orqueio-bpm-platform.job-executor.keepalive-time=The max time an idle thread is kept alive. Defaults to '10' seconds. Unit is seconds.
orqueio-bpm-platform.job-executor.allow-core-timeout=The core threads are also affected by keepalive timeout. Defaults to 'true'.

orqueio-bpm-platform.job-executor.job-acquisitions=A job acquisition.
#orqueio-bpm-platform.job-executor.job-acquisitions.add=Add a job acquisition to the job executor.
#orqueio-bpm-platform.job-executor.job-acquisitions.remove=Remove a job acquisition fromm the job executor.

orqueio-bpm-platform.job-acquisition=A job acquisition.
orqueio-bpm-platform.job-acquisition.name=The unique name of the job acquisition.
orqueio-bpm-platform.job-acquisition.acquisition-strategy=Deprecated. Used to switch acquisition strategy for the job executor. Defaults to 'SEQUENTIAL'.
orqueio-bpm-platform.job-acquisition.properties=Customize the job-acquisition through various properties.
orqueio-bpm-platform.job-acquisition.add=Add a job acquisition to the job executor.
orqueio-bpm-platform.job-acquisition.remove=Remove a job acquisition fromm the job executor.