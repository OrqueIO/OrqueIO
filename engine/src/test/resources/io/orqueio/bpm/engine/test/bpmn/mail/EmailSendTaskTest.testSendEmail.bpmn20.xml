<definitions 
  xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
  xmlns:orqueio="http://orqueio.io/schema/1.0/bpmn"
  targetNamespace="Examples">

  <process id="sendMailExample"  isExecutable="true">

    <startEvent id="theStart" />
    <sequenceFlow sourceRef="theStart" targetRef="sendMail" />
    
    <sendTask id="sendMail" orqueio:type="mail">
      <extensionElements>
        <orqueio:field name="from" expression="${sender}" />
        <orqueio:field name="to" expression="${recipient}" />
        <orqueio:field name="subject" expression="Your order ${orderId} has been shipped" />
        <orqueio:field name="html">
          <orqueio:expression>
            <![CDATA[
              <html>
                <body>
                  Hello ${male ? 'Mr.' : 'Mrs.' } ${recipientName},<br/><br/>
                  
                  As of ${now}, your order <b>has been processed and shipped</b>.<br/><br/>
                  
                  Kind regards,<br/>
                  
                  TheCompany.
                </body>
              </html>
            ]]>
          </orqueio:expression>
        </orqueio:field>      
      </extensionElements>
    </sendTask>
    <sequenceFlow sourceRef="sendMail" targetRef="theEnd" />
    
    <endEvent id="theEnd" />

  </process>

</definitions>
